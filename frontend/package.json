{
  "name": "chm-finance-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "CHM Finance static frontend (landing + dashboard pages)",
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i tailwind.src.css -o tailwind.css --minify",
    "watch": "tailwindcss -c tailwind.config.js -i tailwind.src.css -o tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
